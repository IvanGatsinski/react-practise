export default function Title({ name, continent, country }) {
  return (
    <div>
      <h2>Name: {name}</h2>
      <h2>Continent: {continent}</h2>
      <h2>Country: {country}</h2>
    </div>
  );
}
